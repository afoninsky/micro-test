machine:
  environment:
    VERSION: $(echo ${CIRCLE_TAG:-`jq -r '"v" + .version' package.json`})
dependencies:
  override:
    - echo 'export SOMETEST=blabla' > ~/.circlerc
    - echo 'export SOMETEST=blabla' > ~/circlerc
test:
  override:
    - echo $SOMETEST
    - echo ${SOMETEST}
