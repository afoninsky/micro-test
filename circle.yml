machine:
  environment:
    VERSION: $(echo ${CIRCLE_TAG:-`jq -r '"v" + .version' package.json`})
dependencies:
  pre:
    - echo 'export SOMETEST=blabla' > ${HOME}/.circlerc
  override:
    - pwd
test:
  override:
    - echo $SOMETEST
