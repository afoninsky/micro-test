machine:
  environment:
    VERSION: ${CIRCLE_TAG:-`jq -r '"v" + .version' package.json`}
dependencies:
  override:
    - echo test default var: $CIRCLE_SHA1, ${CIRCLE_SHA1}
    - echo test version: $VERSION, ${VERSION}
test:
    - pwd
